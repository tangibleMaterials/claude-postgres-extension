[project]
name = "postgres-mcp-extension"
version = "1.0.0"
description = "Claude Desktop Extension for PostgreSQL via postgres-mcp"
requires-python = ">=3.12,<3.14"
dependencies = [
    "postgres-mcp>=0.3.0",
]
